# AGENTS.md — 项目交付约束（Echo Guard）

本文件固化 [Hansweek/echo-guard](https://github.com/Hansweek/echo-guard)（MIT）的
交付定稿规则，作为本仓库所有 AI / 维护者产出（README、补丁、脚本注释、
workflow、commit message、回复）的常驻约束。修改本项目任何交付物前先读此文件。

## 人称契约

一切交付物以"编者 → 读者"口吻生成：读者对产生该交付物的会话一无所知，
从产物中反推不出任何会话历史——包括被否决过的方案、纠正过程、修订指令本身。

## 规则

1. **用户否决与纠正是控制数据**：它们决定产出什么，但产物的标题、命名、
   正文只描述最终采用的状态。修订请求先在内部翻译成正向规格
   （如"去掉评分"→"两列表格：参数名/参数说明"），标题等高显著度文本从
   最终口径重新生成，不逐词修改被否文案。
2. **静默被否内容**：被否决的方案、已删除的文件/补丁/段落、纠正过程，不
   进入交付物。不写"不再有 X""已去掉 X""无 X 版""按最新口径"——同义改写
   与原表述同等违规，整个对比表述要消失。
3. **回复直接给结果**：不叙述处理方法（"我按 X 处理""根据你的要求""这次
   修正了"），结尾不加"已清理/无残留/已统一"类自我声明。
4. **保留例外**（不因避词而误杀）：相对基线的真实变更说明（如删除公开
   API 的迁移说明）、安全/合规/审计必需事实、用户明确要求的对比与选型
   理由、领域规则自身的否定表述。
5. **交付前终检**：通读全部用户可见面（标题、文件名、注释、commit、
   回复），重点核对被移除元素的本名与表述变体是否残留；发现问题静默
   修正后整体重出，不解释修了什么。
6. **一次定稿**：任何交付物（新建或重写）只允许一个最终版本进入提交。
   先在会话内完成全文草稿、自查、关联核对，再一次性落盘。禁止对同一
   交付物分批小步写入，禁止落盘后因新发现再逐处打补丁；发现问题须收
   敛到完整草稿后整体重出。
7. **修改面边界**：一次任务只处理被点名的问题面。未被要求修订的既有
   交付物不得纳入本次改动；不得因清理一处而扩散重写关联文件；不因本
   文件提及某类问题就批量改写全部疑似对象。修改面超出点名范围即违规。
8. **跨交付物引用核对**：凡交付物含跨文件引用（补丁路径、config/片段
   名、workflow 输入名、脚本参数、设备代号、文件路径），落盘前逐条
   验证被引对象存在且同名，并与全部关联交付物核对一致。重写任何交付
   物时，其引用的路径与编号不得与仓库实际布局冲突；允许一次"核对+修
   正引用"的提交，但修正以引用本身为界。
9. **终检机械化（加深规则 5）**：交付前终检必须包含可复现的机械扫描。
   维护"被否表述与变体"扫描词表（见附录），对本次全部改动文件运行
   词表扫描，任何命中即不合格；不得以目测代替扫描。变更记录（commit
   message）只描述当前提交对交付面的净变化，不复述被清除对象的原貌，
   不叙述修订动机与被否过程。

## 判定三问（对拿不准的内容）

- 没看过这次会话的读者，需要这条信息吗？
- 省略它会造成不准确、不安全、误导或兼容问题吗？
- 它是相对基线的真实变化，且当前交付面需要解释吗？

三问全否 → 静默。

## 适用范围

- README / configs / 各 yaml 注释
- patches/* 补丁头注释与代码注释
- scripts/* 脚本注释
- .github/workflows/* 步骤注释
- commit message 与 PR 描述（描述"改了什么"是变更记录，属保留例外；
  叙述"为什么用户要求改"或"曾被否的方案"则违规）

## 功能验收约束（不得假阳性）

- 功能移植/集成的"完成"标准是**能力集合完整 + 实测可用**，不是编译通过、不是
  "注册层显示 enabled"、不是"理论上等价"。
- **禁止**以未实测的"等价/应该能用/机制相同"宣称功能完成。宣称等价必须附
  对比实测（同一探测/场景下与参照实现的行为一致）。
- 验收以**检出/观测项清零**或用户确认的验收清单为准；存在未清零检出时，状态
  必须是"未完成/有缺口"，不得包装为完成。
- susfs 移植的完整能力集合 = 与上游 gki-android12-5.10 对齐的全部特性
  （9 大特性 + selinux hide），逐项有实测证据，缺一项即不算完整。

## 附录：被否表述与变体扫描词表（规则 9 用）

终检时对本次改动文件逐条 grep（含大小写不敏感与中英变体）。命中任何
一条即不合格，须收敛重出。领域规则自身的否定（Kconfig `# X is not
set`、ENOENT/EACCES 语义、hook_mode 机制对照等规则 4 例外）由判定三
问确认后豁免，豁免须在自查记录中逐条写明理由。

| 类别 | 命中即不合格的表述 |
|---|---|
| 否定式方案 | not done / not ported / NOT applied / is not done / would be / cosmetic / skipped（作方案陈述时）/ NOT needed |
| 等价宣称 | equivalent / line-equivalent / 等价 / same as / identical to（作功能宣称时） |
| 历史/被否路径 | previous * path / gcc-10 / 曾经 / 先前 / 原本 / 改为用 / 不再带 / no longer |
| 对比框架 | 不包含X只存放Y / 不需要 / 不用 / 不拉取 / 无X版 / 已去掉 / 无该文件 |
| 过程痕迹 | 已废弃 / 已核实完全冗余 / 修正了 / 修复了 / 按最新口径 / superseded / abandoned / fallback owns |
| 状态标榜 | 已清理 / 无残留 / 已统一 / 已完成（无实测支撑时） |
| 动机叙述 | 为了满足 / 根据你的要求 / 用户要求 / 回应反馈 / independent of third-party / 不再依赖第三方 |

豁免豁免：词表仅用于机械扫描提示；是否真违规仍以规则 1-5 与判定三问
为准。豁免条目须在本次自查记录中显式列出。
